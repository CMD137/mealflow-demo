# MealFlow web local environment template.
# Copy to .env.local when you need to override defaults.

# Local dev defaults to Vite proxy. The proxy target is configured in vite.config.ts.
VITE_API_BASE_URL=/api
