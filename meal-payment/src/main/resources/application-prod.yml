spring:
  sql:
    init:
      mode: never
mealflow:
  payment:
    provider: ${PAYMENT_PROVIDER:alipay-sandbox}
    alipay:
      app-id: ${ALIPAY_APP_ID:}
      private-key: ${ALIPAY_PRIVATE_KEY:}
      public-key: ${ALIPAY_PUBLIC_KEY:}
      notify-url: ${NGROK_URL:}/payments/alipay/callback
